import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * @module CartModule
 * @description Módulo encargado de la gestión del carrito de compras.
 * Aquí se incluirán los componentes y servicios relacionados con la funcionalidad del carrito.
 */
@NgModule({
  declarations: [], 
  imports: [
    CommonModule // Proporciona funcionalidades comunes como *ngIf y *ngFor
  ]
})
export class CartModule { }
